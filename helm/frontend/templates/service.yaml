apiVersion: v1
kind: Service
metadata:
  name: {{ include "mentoring-frontend.fullname" . }}
  labels:
    {{- include "mentoring-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mentoring-frontend.selectorLabels" . | nindent 4 }}
